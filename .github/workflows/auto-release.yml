name: Auto Release Linux+Windows

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-release:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            ext: ""
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            ext: ".exe"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 安装 Rust
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      # Linux/Windows 构建
      - name: Build Release
        run: cargo build --release --target ${{ matrix.target }} --all-features

      # 打包
      - name: Package Binary
        run: |
          mkdir -p dist
          cp target/${{ matrix.target }}/release/vgen${{ matrix.ext }} dist/vgen-${{ matrix.target }}${{ matrix.ext }}

      # SHA256 校验
      - name: Generate SHA256
        shell: bash
        run: |
          cd dist
          for f in *; do
            sha256sum "$f" >> SHA256SUMS.txt
          done

      # 上传 artifact，确保名字唯一
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: vgen-${{ matrix.target }}-${{ github.run_id }}
          path: dist/vgen-${{ matrix.target }}${{ matrix.ext }}

      # 生成版本号
      - name: Set Version
        run: echo "VER=v$(date +'%Y%m%d-%H%M')" >> $GITHUB_ENV

      # 上传 transfer.sh
      - name: Upload to transfer.sh
        run: |
          echo "## Anonymous Download Links" > downloads.md
          for f in dist/*; do
            url=$(curl --silent --upload-file "$f" "https://transfer.sh/$(basename $f)")
            echo "- $(basename $f): $url" | tee -a downloads.md
          done

      # 创建 GitHub Release
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.VER }}
          name: Release ${{ env.VER }}
          files: |
            dist/*
            downloads.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
